// import { create } from "zustand";
// import useCalculatorStore from "../calculator/calculatorStore";

// interface OldDataType {
//   oldDataCheck: boolean | undefined;
// }

// // ✅ Use `getState()` to fetch initial state
// const check = useCalculatorStore
//   .getState()
//   .components.some((element) => element.label !== "?");

// const useOldDataStore = create<OldDataType>(() => ({
//   oldDataCheck: check,
// }));

// export default useOldDataStore;
